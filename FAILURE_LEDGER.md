# Failure ledger

Record the first failing checker block, exact statement, command, elapsed time,
classification, and next materially different attempt.  A false invariant is
preserved with its smallest executable or logical counterexample and is not
retried under a new name.

| ID | Stage | Claim/action | Evidence | Class | Disposition |
|---|---|---|---|---|---|
| F-0001 | toolchain discovery | `isabelle build -n` would finish within 30 s | outer shell timed out while AFP sessions were still being scanned; the exact project-owned Java/bash tree was terminated, and pre-existing Isabelle processes were untouched | B_ENVIRONMENT | rerun only through the project harness with a larger bounded timeout and durable logs |
| F-0002 | list smoke harness preflight | Windows launcher existed at `bin/isabelle` rather than the assumed `bin/isabelle.exe` | `build-list-smoke.ps1` stopped before starting a process: `Required path is missing: C:\Isabelle2025-2\Isabelle2025-2\bin\isabelle.exe` | B_ENVIRONMENT | invoke the extensionless Isabelle tool through the installation's Cygwin `bash.exe`; no CParser run occurred |
| F-0003 | list smoke harness path conversion | Cygwin path arguments would contain only `/` separators | run `20260731Tlist-smoke-02`, exit 2 in 3.478 s: `Illegal character "\\" in path element`; the conversion replaced doubled rather than single backslashes | B_ENVIRONMENT | replace each single Windows separator before invoking Isabelle; no session or CParser run occurred |
| F-0004 | AFP session registration | registering only the `AutoCorres2` entry would make its direct AFP parents visible | run `20260731Tlist-smoke-03`, exit 2 in 4.556 s: `Bad parent session "Simpl" for "AutoCorres2"` | B_ENVIRONMENT | register the direct generic AFP entries `Simpl` and `Word_Lib` without scanning all AFP sessions; CParser did not run |
| F-0005 | CParser preprocessor launch | `cpp_path` would resolve relative to the theory file | run `20260731Tlist-smoke-04`, exit 1 in 35.424 s at `install_C_file`: temporary bash reported `../../scripts/cpp-wsl.sh: No such file or directory` | B_ENVIRONMENT | use the toolchain-locked absolute Cygwin bridge path; upstream source and macro contract unchanged |
| F-0006 | CParser sanitized include closure | `new_C_include_dir` alone would make original directory contents visible inside CParser's isolated temporary tree | run `20260731Tlist-smoke-05`, exit 1 in 23.691 s at `install_C_file`: WSL cpp found the copied `list.c` but reported `FreeRTOS.h: No such file or directory` | B_FRONTEND | explicitly register the seven custom headers in the real transitive include closure with `include_C_file`; no header or source-body rewrite |
| F-0007 | host libc contamination | the WSL host's glibc declarations would be a suitable CParser model of the target's freestanding standard-header surface | run `20260731Tlist-smoke-06`, exit 1 in 25.701 s at `install_C_file`; exported `list.i` was 206,465 bytes and contained GNU `__extension__`, attributes, asm aliases, builtins, and host 64-bit typedefs | B_FRONTEND | supply audited minimal `stddef.h`/`stdlib.h` stubs for exactly `size_t` and `NULL`; no executed libc call is removed |
| F-0008 | session directory layout | two sessions declared in one ROOT could share the ROOT's base directory while using different theory subdirectories | run `20260731Tmodel-01`, exit 2 in 4.573 s: duplicate use of `theories` by list-smoke and model sessions | B_ENVIRONMENT | place the sessions themselves `in "list_smoke"` and `in "model"`; each source directory has exactly one owner |
| F-0009 | AutoCorres program key | AutoCorres would use the full source path as the CParser program key | run `20260731Tlist-smoke-07`, exit 1 in 29.786 s: `install_C_file` succeeded, then AutoCorres reported that the full relative filename had not been parsed | B_FRONTEND | invoke AutoCorres with CParser's registered basename `list.c`; this changes no translation option or source token |
| F-0010 | split-heap addressability | the embedded list sentinel could remain a non-addressable split-heap field despite upstream taking and type-casting its address | run `20260731Tlist-smoke-08`, exit 1 in 29.566 s: AutoCorres required `xLIST_C.xListEnd_C` to be addressable | B_PROOF_ARCH | use documented `addressable_fields = xLIST.xListEnd`; do not ignore the error or assume away the alias |
| F-0011 | port prototypes without bodies | declarations pulled in by official `portable.h` would have implementations in the `list.c` translation unit | run `20260731Tlist-smoke-09`, exit 1 in 37.433 s after addressable-field setup: first missing implementation was `vPortFree` | B_FRONTEND | declare all seven uncalled port prototypes via `no_body`; their nondeterministic operations are unreachable from every `list.c` body |
| F-0012 | generated-signature reporting | `print_statement` would accept generated function constants directly | run `20260731Tlist-smoke-11-signatures`, exit 1 in 44.843 s after AutoCorres completed: each bare `vList...\<apostrophe>` was reported as an undefined fact | B_LOCAL_PROOF | print the five generated definition facts `vList...\<apostrophe>_def`, whose equations expose the constants and signatures |
| F-0013 | session-store reporting | exporting `ISABELLE_HOME_USER` immediately before the Windows launcher would relocate its database | runs through `list-smoke-12-final` recorded the requested path, but the actual database is `%USERPROFILE%\.isabelle\Isabelle2025-2\heaps\polyml-5.9.2_x86_64_32-windows\log\EAL6_FreeRTOS_V611_List_Smoke.db`; the requested directory stayed empty | B_ENVIRONMENT | remove the ineffective export, report the actual default store, and serialize builds; use `USER_HOME` only in a separately rebuilt private-store revision |
| F-0014 | combined scheduler TU order | `list.c` could precede `tasks.c` without changing include semantics | GCC syntax audit then saw scheduler API bodies renamed to `MPU_*` and `pxCurrentTCB` undeclared: `FreeRTOS.h`/`mpu_wrappers.h` had already been include-guarded before `tasks.c` defined `MPU_WRAPPERS_INCLUDED_FROM_API_FILE` | B_FRONTEND | include unmodified `tasks.c` before unmodified `list.c`; prototypes permit the list definitions to follow, and no upstream token changes |
| F-0015 | non-MPU port selection | defining `portUSING_MPU_WRAPPERS` to numeric `0` would select the non-MPU branch | preprocessor audit produced 67 GNU section attributes and redefinition warnings because upstream tests macro presence with `#ifdef` | B_FRONTEND | leave the macro absent before `mpu_wrappers.h`; its fallback defines the observable value `0` and empty privilege annotations |
| F-0016 | abstract-model source syntax | Unicode glyphs and a raw cartouche delimiter copied through the Windows editing path would be accepted as Isabelle symbols | runs `20260731Tmodel-02` through `model-04` failed in 10.355--10.425 s with an outer-syntax error at line 5 and then an inner lexical error at the record arrow on line 16 | B_FRONTEND | encode every Isabelle symbol as an ASCII escape such as `\<open>` and `\<Rightarrow>`; a raw-non-ASCII scan now stays empty |
| F-0017 | monolithic model proof search | broad `auto`/`metis` calls would either solve or promptly expose all source-derived invariant obligations | `model-05` and `model-06` kept PolyML at a full core and about 5.8 GB until their 300 s / 150 s bounds; the normally terminated `model-07-locate` PIDE export exposed unfinished goals at `distinct_insert_after`, `predecessor_aux_member`, `predecessor_member`, `list_insert_end_preserves_wf`, and `list_remove_preserves_wf`, plus a premature document antiquotation for `ring` | B_PROOF_ARCH | replace the failed automation by explicit induction/case bricks and split the canonical model into `Definitions` -> `Sequence_Lemmas` -> `Predecessor_Lemmas` -> `Invariants` -> executable witnesses |
| F-0018 | Cygwin timeout cleanup | killing the Windows PID returned for Cygwin `bash.exe` would reliably terminate an Isabelle build tree | after the outer caller left `model-05`/`model-06`, `taskkill /PID <bash> /T /F` reported unsupported Cygwin children while the Java and PolyML processes remained live; the exact model Java/PolyML PIDs were therefore identified and terminated separately | B_ENVIRONMENT | use Isabelle's session `timeout` as the primary bound until the harness gains a Cygwin-aware process-group cleanup; never infer that an outer shell timeout released the build slot |
| F-0019 | list heap-lift proof readiness | a checker-green AutoCorres translation would imply that the embedded-sentinel alias had been fully eliminated | the final PIDE stream warns that `field_lvalue` could not be removed from `vListInitialise` and `vListInsert` and that their output may be unprovable; the same stream records `unchanged_typing` failures for all seven unreachable `no_body` port operations | B_PROOF_ARCH | retain the green result only as a translation smoke; make sentinel/common-prefix aliasing explicit in the representation relation, and replace every reached port stub by a reviewed contract before claiming refinement |
| F-0020 | first split-model checker rung | the structured `distinct_insert_after` proof's head-equals-cursor branch would close by simplification alone | `20260731Tmodel-08-rungs` was bounded by the 60 s Isabelle session timeout; its PIDE exports reported zero errors in Definitions, Predecessor, Invariants, and witness theories, and one Sequence_Lemmas subgoal: assumptions contained `x \<noteq> c` while the goal required `c \<noteq> x` | B_LOCAL_PROOF | add the explicit symmetry brick `c_ne_x: c \<noteq> x` before simplification; keep the five-theory checker dependency instead of returning to monolithic search |
| F-0021 | stable ordered-insert proof | induction followed by unrestricted `auto` would establish that stable insertion preserves sorted keys | after the distinctness symmetry bricks, sequential run `20260731Tmodel-11-sequential` reached the 60 s session bound; its Sequence_Lemmas PIDE stream ends at the `sorted_stable_key_insert` theorem statement with no proof completion, identifying that proof future as the first non-returning command | B_PROOF_ARCH | split on the head-key comparison, derive the two head-to-tail bounds explicitly, and invoke only the exact induction hypothesis for the recursively inserted tail |
| F-0022 | predecessor survives removal | `blast` would promptly use `set_remove1_subset` and the induction hypothesis to retain predecessor membership | sequential `20260731Tmodel-12-sequential` completed every Sequence_Lemmas command, then reached the 60 s bound with the Predecessor_Lemmas PIDE stream stopping in the `previous = x` branch of `predecessor_aux_survives_remove1`, immediately before the `blast` method | B_LOCAL_PROOF | name the removed-set subset fact and apply `subsetD` directly to the exact induction-hypothesis membership; `20260731Tmodel-13-sequential` then validated the full five-theory chain with exit code 0 |
| F-0023 | `Raw-R0` fixed-address guards | the raw guard witness would close after importing the raw translation and simplifying the three concrete intervals | `20260731Tlist-raw-r0-01-guards` (15.816 s) lacked the parent-session-qualified import; `-02-guards` (58.447 s) used unavailable method `norm_num`; `-03-guards` (20.963 s) left `4116 <= CARD(32 word)` without `card_word`; `-04-guards` (21.724 s) proved the individual guards but the final broad `simp` left the cast-sentinel `c_guard` | B_ENVIRONMENT + B_LOCAL_PROOF | qualify the parent import, use explicit interval/no-wrap bricks and the exact three guard lemmas; `20260731Tlist-raw-r0-05-guards` is green in 21.774 s with `quick_and_dirty=false` |
| F-0024 | `Raw-R1` list initialisation | generic simplification would expose all five raw post-state fields and a disjoint canary frame after `vListInitialise'` | `20260731Tlist-raw-r1-01-init` (37.613 s) left five `hrs_mem` read-after-update fields; `-02-init` (25.141 s) closed count but left four nested self-updates; `-03-init` (22.492 s) closed the exact theorem and then failed on a surplus `apply simp` with `No subgoals!`; `-04-init` (21.751 s) left the canary frame under nested `hrs_mem_update (heap_update ...)` | B_LOCAL_PROOF | use directed read-after-write helpers, remove the surplus tactic, and unfold `hrs_mem_update` only for the frame; `20260731Tlist-raw-r1-05-init` is green in 23.832 s with exact-post and canary-frame theorems |
| F-0025 | `Raw-R2` item initialisation | interval arithmetic and generic heap-update simplification would prove the exact item update plus item/list/canary frames | `20260731Tlist-raw-r2-01-init-item` (38.170 s) first failed at `raw_list_item_intervals_disjoint`; `-02-init-item` (23.352 s) reached the same arithmetic brick with an unconstrained numeral type; `-03-init-item` (21.693 s) passed interval, canary, and item-update goals but left `raw_list_h_val_after_item_update` at the `from_bytes (heap_list (heap_update_list ...))` frame | B_LOCAL_PROOF | type the arithmetic explicitly and prove the raw list `h_val` frame with the exact disjoint-update lemma; `20260731Tlist-raw-r2-04-init-item` is green in 22.001 s with `raw_vListInitialiseItem_exact_post_and_frames` |
| F-0026 | split-heap sentinel non-vacuity bricks | generic simplification would derive the typed-root contradiction directly from the two overlapping 20-byte root intervals | `20260731Tm0-bridge-03-guard-conflict` (19.760 s) left both the interval-intersection contradiction and the two-root implication; `-04-explicit-overlap` (19.440 s) localized the first failure to proving `ptr_val list + word_of_nat 8` belongs to the second interval; `-05-typed-root-conflict` (20.636 s) then failed on a proof-mode wrapper with `Illegal application of proof command ... at have` | B_LOCAL_PROOF | normalize the word offset, give the explicit overlap witness, and repair the structured proof; `20260731Tm0-bridge-06-root-guard` is green in 20.224 s for the guard conflict and `20260731Tm0-bridge-07-operational-top` is green in 20.947 s for the sentinel execution reaching `top`/having no normal run |
| F-0027 | `Raw-R3b` sentinel write locality | one broad simplification would both normalize the cast whole-item writes and prove preservation of the eight bytes beyond the real list object | among 19 bounded calls, `20260731Tlist-raw-r3b-05-next-tail8` exposed the unclosed bounded `heap_update_list` frame, `-08-mini-normalize` exposed the concrete/cast mini-pointer equality, `-12` through `-15` exposed the whole-item-to-embedded-list update equations, and `-18-selector-frames` left the opposite link selector | B_LOCAL_PROOF | prove typed field addresses, pointwise disjoint byte frames, embedded-list normalizers, and selector frames separately; `20260731Tlist-raw-r3b-19-selector-frames` is green in 22.432 s |
| F-0028 | `Raw-R3c` exact prestate | nested raw heap construction would expose all list/item projections and heap typing by unfolding alone | runs `20260731Tlist-raw-r3c-01` through `-05-prestate` successively exposed malformed equality syntax, the disjoint list read after the item update, the `hrs_mem`/`hrs_htd` pair projection, and an escaping local abbreviation; all five exited 1 | B_FRONTEND + B_LOCAL_PROOF | define the list and item values explicitly, reuse the exact disjoint `h_val` frame, and state the witness fields without an escaping abbreviation; `20260731Tlist-raw-r3c-06-prestate` is green in 22.666 s |
| F-0029 | `Raw-R3d` positive insert reachability | unfolding `vListInsertEnd'_def` and running one generic VCG simplification would establish a nonempty result at the concrete witness | after the initial syntax error, runs `20260731Tlist-raw-r3d-02` through `-04-result` left the cursor/sentinel guard after the item next/previous writes as a nested raw-heap goal | B_LOCAL_PROOF | isolate the exact prestate index and sentinel-next facts, prove the guard survives the preceding item updates, and feed those bricks to the bounded VCG; `20260731Tlist-raw-r3d-05-result` is green in 24.132 s |
| F-0030 | `Raw-R3e`/`Raw-R3f` effect posts | one enlarged postcondition VCG would simultaneously recover count, cursor, all four links, and the container from the nested write sequence | `20260731Tlist-raw-r3e-02-post-goal` left count under nested item/sentinel updates; `20260731Tlist-raw-r3f-03-topology-goals` left the combined topology readbacks; later red topology calls localized the sentinel and item selector frames | B_PROOF_ARCH + B_LOCAL_PROOF | prove read-after-write/frame normalizers first and check count/index, sentinel links, item links, and container as separate postconditions; final runs `r3e-04-count-index-post`, `r3f-06-sentinel-links`, `r3f-08-item-links`, and `r3f-09-container` are green |
| F-0031 | `Raw-R3g` frames and R3 master | generic simplification would preserve key/owner, lift a pointwise tail frame to a bounded universal post, preserve canary/typing, and then justify one monolithic strengthened VCG | `20260731Tlist-raw-r3g-01`/`-02-key-owner` left item selectors, `-06`/`-07-tail-all` left the quantified `runs_to` goal, and `-09-canary-htd` ended with a surplus tactic after the goal had already closed | B_PROOF_ARCH + B_LOCAL_PROOF | export separate frame VCGs, derive the universal tail post with `runs_to_all`/weakening, and mechanically combine eight already-green groups with `runs_to_conj`; `20260731Tlist-raw-r3g-10-canary-htd` and `20260731Tlist-raw-r3-master-01` are green, with no second monolithic VCG |
| F-0032 | `Raw-R5` fixed relation/simulation | a new raw relation theory could import both proof staircases and discharge the empty/singleton bridge by direct simplification | ten bounded calls total 208.138 s: `r5-01`/`r5-03` exposed ROOT dependency-order and duplicate-session errors; `r5-02`/`r5-04` left fixed layout guards; `r5-05`/`r5-07`/`r5-08` exposed malformed statements; `r5-06` required deriving the real node differs from the sentinel; `r5-09` misused proof commands after naming the master theorem; all nine exited nonzero | B_ENVIRONMENT + B_FRONTEND + B_LOCAL_PROOF | register the model as an additional session dependency, prove explicit empty/singleton layout and relation introduction bricks, parenthesize the `runs_to` state, and finish the weakened master result with an explicit `show`; `20260731Tlist-raw-r5-10-show-thesis` is green in 25.236 s for the fixed empty-to-singleton simulation only, not a general-N relation theorem |
| F-0033 | frozen P2 addressed-data non-vacuity | the ordinary upstream CParser translation would by itself bind scheduler data-global constants to the linked ELF addresses | the ordinary translation permits null or overlapping interpretations; `20260801Tseal-p2-layout-no-go-01-portable` preserves that limitation as a checker-green no-go theorem (33.162 s, exit 0, `quick_and_dirty=false`) | B_MODEL_BOUNDARY | keep the ordinary no-go result and use the separately named, project-local audited addressed-global patch; the external builder/generator hash-locks ELF `DC830E50513384D712E0D1C68CB198EA656365F673D021C452D7D7EBD45C045A`, ledger `CA288A4CD2344BE979ADFA9DBF0298C6715F196D64AE472D173304289C4F2C02`, and config `27F74768E1DB1C3F8DBFCFC85371075192BB7D2544ED324DC81B65A9A2911712`, while exact-map CParser definitionally fixes the generated addresses and the static-nine, dynamic-TCB, and preimage sessions prove the source-semantic obligations without modifying installed AFP; the external correspondence is not an Isabelle theorem |
| F-0034 | literal four-call acceptance chain | the final chain should additionally be advertised as preserving all eight bytes beyond the 20-byte list object | the checked theorem `raw_vListInitialise_insert_end_remove_refines` states the relation result only; its proof has exactly three `runs_to_bind` applications and no assumption, but no chain-level `tail8` postcondition | B_SCOPE | accept the literal functional hard gate and make no `tail8` claim; `20260801Tseal-list-four-call-01-portable` is green in 32.338 s with exit 0 and `quick_and_dirty=false` |
